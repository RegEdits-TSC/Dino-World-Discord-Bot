name: pr-title

# Validates that every PR title follows Conventional Commits.
# Catches mistakes at PR-open time, before squash-merge bakes a bad
# subject into main's history.
on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read
  statuses: write   # action v6 publishes a commit status for the title check

jobs:
  lint:
    name: conventional commit title
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Lint PR title
        uses: amannn/action-semantic-pull-request@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            refactor
            chore
            docs
            test
            perf
            style
          requireScope: false
          subjectPattern: ^[A-Za-z].*[^.]$
          subjectPatternError: |
            Subject must start with a letter and not end with a period.
            Got: "{subject}"
          wip: true
