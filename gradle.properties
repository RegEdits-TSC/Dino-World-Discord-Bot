# Gradle 9 + JDK 21+ prints a "restricted method" warning because its
# native-platform library calls System.load() without the JVM having native
# access enabled for unnamed modules. Suppress it by enabling native access
# on the Gradle daemon — the warning is about Gradle internals, not our code.
org.gradle.jvmargs=--enable-native-access=ALL-UNNAMED
